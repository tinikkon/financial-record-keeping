<?php

declare(strict_types=1);

return [

    /*
     * Здесь есть только открытый ключ: выпустить токен этот сервис не может
     * и не должен. Проверка подписи идёт на месте, без обращения к соседу,
     * поэтому недоступность сервиса таблиц не мешает читать историю.
     */
    'public_key_path' => env('JWT_PUBLIC_KEY_PATH', '/run/secrets/jwt-public.pem'),
    'algorithm' => 'RS256',

];
