# Состояние работ

Обновляется после каждого завершённого этапа. План — `plans/2026-09-18-implementation.md`.

## Сделано

- **Проектное решение** — `specs/2026-09-18-design.md`.
- **План реализации** — `plans/2026-09-18-implementation.md`.
- **Этап 1. Каркас репозитория.** `docker-compose.yml` на девять контейнеров,
  образ PHP 8.4 на FrankenPHP с расширением `mongodb`, `Makefile`, `.env.example`.
  MongoDB поднимается набором реплик из одного узла — без этого нет ни транзакций,
  ни потоков изменений. Образ собирается успешно.
- **Этап 2. Движок формул** — `packages/formula-engine`. Лексер, парсер, дерево
  разбора, вычислитель на точной десятичной арифметике, функция суммы, извлечение
  зависимостей, планировщик пересчёта по алгоритму Кана с обнаружением колец ссылок.
  **51 тест зелёный, PHPStan на уровне max без замечаний.**

- **Этап 3. Сервис таблиц: каркас и вход.** Laravel 13.32 на MongoDB, слой `Core`
  (базовый репозиторий, объявление индексов рядом с репозиторием, команда их создания),
  домен `Auth`: вход по почте и паролю, токен доступа на асимметричной подписи,
  токен обновления с одноразовым применением и самоочисткой по индексу срока жизни,
  посредник проверки токена, команда заведения пользователя.
  **15 тестов зелёные, проверены на настоящей MongoDB.**

## В работе

- **Этап 4** — книги и листы: модели, репозитории, копия месяца, версия листа.

## Дальше по плану

Этапы 5–14: сервис таблиц с входом и ячейками, живая рассылка, очередь,
сервис истории, фронтенд, сквозные тесты, сборка и развёртывание.

## Принятые решения, о которых легко забыть

- Разделитель аргументов в формулах — и `;`, и `,`; дробная часть — всегда через точку.
- Порядок унарного минуса и степени взят из Excel: `-2^2` равно четырём.
- Имена функций работают по-русски и по-английски, регистр не важен.
- Ячейки — отдельная коллекция, а не массив внутри листа. Причины в спеке.
- Диапазоны в зависимостях хранятся границами, а не списком ячеек.
- Порты окружения: 8088 и 8448.
- Тесты сервисов ходят в настоящую MongoDB, база `finance_sheets_test`.
- Ключи подписи токенов лежат в `docker/secrets/`, в репозиторий не попадают,
  создаются командой `make keys`.
- Заведённые локально пользователи: `kostya@example.com` и `wife@example.com`,
  пароль `finance-local-8`.
